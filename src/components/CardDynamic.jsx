function CardDynamic(props) {

    return (
      <>
        <h2>{props.title}</h2>
        <p>{props.description}</p>
        <a href="">{props.link}</a>
      </>
    )
  }
  
  export default CardDynamic